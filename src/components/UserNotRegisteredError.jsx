import React from 'react';

const UserNotRegisteredError = () => {
  return (
    <div>
      <p>helia and lucky forever! need to register to use the application.</p>
    </div>
  );
};

export default UserNotRegisteredError;